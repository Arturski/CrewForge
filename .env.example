# CrewForge configuration (copy to .env). All optional for dry-run mode.

# Control plane
CREWFORGE_HOST=127.0.0.1
CREWFORGE_PORT=8765

# Live LLM providers (P1 — dry-run mode needs none of these).
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# Or point at a local model (Ollama, etc.) once live runs land.
